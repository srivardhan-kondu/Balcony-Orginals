# Runtime dependencies only — everything server.py actually imports.
# Dev, test and image-generation tooling lives in requirements-dev.txt.
fastapi==0.110.1
uvicorn[standard]==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4,<3
python-dotenv>=1.0.1
